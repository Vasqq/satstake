# Secrets (LLR-DP-010). The testnet key lives in .env; mainnet keys stay in Foundry keystores outside the repo.
.env*
!.env.example
*.pw
*.key
*.pem
keystore/
keystores/
*.keystore
UTC--*
# The burner keystores are named after their accounts; ignore them if ever copied in.
satstake-deployer
satstake-referee

# Foundry
cache/
broadcast/
out/

# Node
node_modules/
dist/
coverage/
lcov.info
*.log

# Claude Code personal overrides
.claude/settings.local.json

# Editor and OS
.DS_Store
._*
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~
